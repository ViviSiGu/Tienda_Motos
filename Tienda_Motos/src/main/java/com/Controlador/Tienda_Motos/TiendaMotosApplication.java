package com.Controlador.Tienda_Motos;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

//import com.DAO.Tienda_Motos.Conexion;

@SpringBootApplication
public class TiendaMotosApplication {

	public static void main(String[] args) {
		SpringApplication.run(TiendaMotosApplication.class, args);
		
		// Crear variable de connection  de acuerdo a la clase del paquete DAO
		
		//Conexion Conectar = new Conexion();
		//Conectar.getConnect();
		
		
		
	}

}
